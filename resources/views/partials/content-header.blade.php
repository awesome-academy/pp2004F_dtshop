<div class="page-header">
    <h2 class="header-title">{{$name.' '.$key}}</h2>
    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a href="{{ route('admin.home') }}" class="breadcrumb-item"><i class="ti-home p-r-5"></i>Home</a>
            <a class="breadcrumb-item" href="#">{{$name}}</a>
        </nav>
    </div>
</div>